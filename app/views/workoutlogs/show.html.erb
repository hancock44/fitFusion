<h1><%= @workoutlog.day %></h1>
<h2><%= @workoutlog.type %></h2>

<p><%= @workoutlog.description %></p>
<div>
  <%= link_to "Edit", edit_workoutlog_path(@workoutlog) %>
</div>
<div>
  <%= link_to "Destroy", workoutlog_path(@workoutlog), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
</div>
